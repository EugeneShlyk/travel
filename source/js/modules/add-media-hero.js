const slides = document.querySelectorAll('.hero-card');
const video = slide.querySelector('.hero-card__video');
const audio = slide.querySelector('.hero-card__inner-container-audio');

// export const addMediaSrc = () => {
//   slides.forEach((slide) => {
//     let childSlides = slide.childNodes;
//     childSlides.forEach((childSlide) => {
//
//     })
//   });
// };

export const addMediaSrc = () => {

};


// проверяет объект на свойство
// obj.hasOwnProperty(prop)
